import { injectable, registry, inject, scoped } from "tsyringe";

export const diInjectable = injectable;
export const diRegistry = registry;
export const diInject = inject;
export const diScope = scoped